# OpenMetrics Explorer (prototype)

Single-file browser tool for exploring an Estuary task's OpenMetrics endpoint.
Paste a CLI-API access token (from Admin → CLI-API in the dashboard) and it
queries `agent-api` directly from the browser — no backend of its own.

This branch exists only to serve the tool via GitHub Pages for testing. Not
merged into `main`.
